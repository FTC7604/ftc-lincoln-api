package com.lhsrobotsimulator;

public class LincolnRobot
{
    public RobotSimulator simulator;

    double x = 50, y = 50, rotation, moveSpeed, rotationSpeed;
    // distance in cm, rotation in degrees, movement speed in cm/s, rotation speed in deg/s
    int width = 32, height = 32;
    // length in cm
}
